version: '3.7'

services:
  database:
    image: postgres:latest
    networks:
      development:
        aliases:
          - database
    ports:
      - "5432:5432"
    environment:
      - "POSTGRES_USER=postgres"
      - "POSTGRES_PASSWORD=local"

networks:
  development:
    name: development